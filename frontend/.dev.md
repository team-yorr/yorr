# 프론트엔드 자동 QA 기록

> 브랜치: `frontend/test/run-auto-qa` (워크트리, `frontend/base` 기준)
> 일시: 2026-07-30 · 환경: Vite dev 서버 + MSW `mock` 모드(REST=MSW, WS=FakeRealtimeClient) · Chromium 1280×720 / 375×812

## 요약

**1차(시나리오) QA**: 8개 시나리오 전부 핵심 흐름 통과. 실제 코드 버그 1건(FND-4, 모바일 대기실 가로 스크롤), 문구·표시 불일치 경미 2건(FND-1, FND-2), UI 전이 상태 불일치 경미 1건(FND-3)을 발견했고, mock 픽스처 자체의 한계 2건은 별도로 구분해 기록했다.

**2차(사용자 제보) QA**: 사용자가 직접 플레이하며 짚어낸 6건을 라이브로 재검증. 실제 버그 2건 확정(FND-5 점수시트가 턴 전환에도 안 닫힘, FND-6 대기실·결과 화면 세로 전체 스크롤), UI 폭 기준 불일치 1건(FND-8), UX 강화 필요성 1건(FND-7, 구현 방향은 확인 필요), 덤으로 조사 오류 1건(FND-9) 발견. 사용자 제보 4·6번은 기존/신규 항목과 겹쳐 별도 항목을 만들지 않고 통합했다.

**수정 구현**: FND-1·2·3·4·5·6(대기실)·8·9는 수정 완료, 5개의 작은 커밋으로 분할. FND-6 중 GameResult와 FND-7의 강한 연출 옵션은 방향 확인 후 후속 작업으로 남겨뒀다 — 자세한 설계·완료·잔여·한계는 맨 아래 "수정 구현" 절 참고.

**후속 구현(같은 날 2차)**: 남겨뒀던 항목 전부 완료 — FND-6 GameResult 스크롤 고정, FND-7 세 옵션(a·b·c) 모두 구현, mock 픽스처 한계 2건(재접속 스냅샷·정적 점수 후보값)도 해소. 커밋 3개 추가. 아래 "후속 구현" 절 참고.

## 시나리오 목록

| # | 시나리오 | 상태 |
|---|---|---|
| S1 | 랜딩 레이아웃·코드 폼 검증 | 통과 |
| S2 | 초대 코드 참가 경로 (`/join?code=`) | 통과 |
| S3 | 닉네임 입력 검증 | 통과 |
| S4 | 방 생성 → 로비 | 통과 |
| S5 | 게임 플레이 코어 루프 | 통과(관찰사항 1건) |
| S6 | 세션 복원·나가기 | 통과(mock 한계 1건 확인) |
| S7 | 오류 경로 (없는 방·404) | 통과 |
| S8 | 모바일 뷰포트 반응형 | 통과(버그 1건 발견) |

## 발견 사항

### OBS-1. 뷰포트 리사이즈 직후 레이아웃 미갱신 (재검증 완료 — 재현 안 됨)
- 최초 관찰: 뷰포트 1280px인데 `matchMedia('(min-width: 760px)').matches === true`이면서 화면은 narrow 레이아웃으로 보인 순간 포착.
- 재검증: S8 진행 후 375→1280 리사이즈로 되돌려 확인한 결과 `wideMatch: true` + wide 레이아웃 정상 렌더. 자동화 도구가 Browser pane이 아직 표시되지 않은 상태(스크린샷 실패 로그 참고)에서 찍은 스냅샷이었을 가능성이 높음 — `useMediaQuery`/앱 코드 문제로 보지 않음.

### FND-1. 닉네임 최소 길이: 안내 문구 "2~12자" vs 실제 검증 1자 허용 (경미)
**상태: 수정 완료** — 문구를 "1~12자"로 통일(검증 로직은 변경 안 함). [`NicknamePage.tsx`](src/screens/NicknamePage.tsx)
- 재현: `/join?code=YORR64`에서 닉네임 "가"(1자) 입력 → 통과, 대기실 입장됨.
- 원인: [`src/nickname.ts`](src/nickname.ts)의 `getNicknameError`는 빈 값만 거부(최소 1자). NicknamePage 안내 문구는 "한글·영문·숫자 2~12자".
- 제안: 기획 확정값에 맞춰 둘 중 하나 통일(검증에 최소 2자 추가 또는 문구를 1~12자로).

### FND-2. 초대 코드 힌트 문구 "4자" vs 검증 "4~12자" (경미)
**상태: 수정 완료** — 힌트를 "4~12자예요"로 통일. [`InvalidInvitePage.tsx`](src/screens/InvalidInvitePage.tsx)
- 재현: `/join?code=AB` → InvalidInvitePage. 힌트 "코드는 영문과 숫자 4자예요", 에러 문구는 "4~12자".
- 실제 패턴은 `^[A-Z0-9]{4,12}$` ([`src/roomCode.ts`](src/roomCode.ts)). 힌트가 4자 고정으로 읽힘.

### FND-3. 카테고리 제출 직후 헤더가 "내 턴이에요"를 계속 표시 (경미, 전이 상태)
**상태: 수정 완료** — `isMyTurn && !submitted`일 때만 "내 턴이에요", `submitted`면 "제출 완료 · 대기 중"으로 분기. vitest로 회귀 테스트 추가. [`GamePlay.tsx`](src/screens/GamePlay.tsx)
- 재현: 방장으로 3회 굴림 후 "라지 스트레이트"(0점 아닌 칸) 확정 → 하단에는 "점수가 반영됐습니다. 다음 턴을 기다립니다." 배너가 뜨지만, 상단 헤더는 여전히 "내 턴이에요"·초록 점을 표시.
- 원인: [`src/screens/GamePlay.tsx:610`](src/screens/GamePlay.tsx:610) 헤더 라벨이 `isMyTurn`만 보고 `submitted`(=`local.phase === 'roundComplete'`)를 고려하지 않음. 서버가 다음 `round.start`를 보내 `activePlayerId`를 갱신하기 전까지는 헤더와 하단 배너가 모순된 문구를 동시에 보여줌.
- 영향: 실제 서버가 즉시 다음 턴을 push하면 순간적이라 체감이 작지만, 지연·재접속 등으로 창이 벌어지면 "내 턴인데 왜 대기하라는거지" 하는 혼란을 줄 수 있음.
- 제안: 헤더 조건에 `!submitted &&`를 추가하거나 `submitted`일 때 "제출 완료 · 대기 중" 등 별도 라벨을 노출.

### 참고. mock 환경의 점수 후보값은 실제 주사위와 무관(픽스처 고정값) — 후속 구현에서 해소
**상태: 해소** — mock이 제출된 주사위로 `domain/scoring`을 사용해 점수를 계산한다("후속 구현" 절 참고).
- `src/mocks/fixtures.ts`의 `scoreCandidates.candidates`가 정적 값이라, 클라이언트가 로컬로 계산해 보여준 "라지 스트레이트 30" 후보와 실제 제출 후 서버(mock)가 반영하는 값(0)이 다를 수 있음.
- 이건 mock 픽스처의 한계이지 FE 버그는 아님 — 서버 응답을 신뢰해 클라 상태를 덮어쓰는 것은 [`docs/current-baseline.md`](docs/current-baseline.md)의 "서버 상태와 불일치 시 스냅샷 기준 교정" 정책과 일치. 실제 서버 연동(E2E) 검증 시엔 재현되지 않을 항목.

### 참고. mock 재접속 시 `room.join` 응답이 항상 대기 중 스냅샷으로 고정됨 — 후속 구현에서 해소
**상태: 해소** — 아래 "참고용 개선 아이디어" 그대로, mock이 방 상태를 기억했다가 `room.join` 때 복원한다("후속 구현" 절 참고).
- 증상: 게임 진행 중 새로고침 → "이어서 하기" → 대기실로 이동 후, 실제로는 라운드 진행·점수 기록이 있었음에도 대기실이 "참가 인원 2/6, 온라인"뿐인 신규 대기 상태로 보임(라운드·점수 정보 소실).
- 원인: [`src/mocks/realtimeScenarios.ts`](src/mocks/realtimeScenarios.ts)의 `room.join` 핸들러가 항상 정적 `creatorSession.snapshot`(=`waitingRoomSnapshot`, phase `waiting`)을 반환. [`src/app/RealtimeSync.tsx`](src/app/RealtimeSync.tsx)의 `keepGameState()`는 "서버가 waiting이라 하면 신뢰해서 그대로 따른다"는 의도된 로직이라, 서버(mock)가 매번 waiting을 우기면 진행 상태가 매번 날아감.
- 실제 서버라면 재접속 시 `room.join` 응답이 현재 진짜 phase(`playing`)를 정확히 돌려주므로 재현되지 않을 것으로 판단됨 — FE 코드 결함이 아니라 mock 픽스처의 정적 응답 한계.
- 참고용 개선 아이디어(선택): mock도 실제 방 상태(마지막 `playingRoomSnapshot`)를 들고 있다가 `room.join` 때 그걸 돌려주면, 새로고침 후 재접속 시나리오를 mock만으로도 온전히 검증할 수 있음.

### FND-4. 대기실 초대 카드가 375px 모바일 폭에서 가로 스크롤 유발 (실제 버그, 중간)
**상태: 수정 완료** — 행·텍스트 컨테이너 둘 다에 `min-w-0` 추가, 방 코드 폰트를 `clamp()`로 가변화하고 `truncate`를 안전망으로 추가. 6자·12자 코드 양쪽 다 375px에서 overflow 없음 확인. [`InvitationPanel.tsx`](src/components/InvitationPanel.tsx)
- 재현: 모바일 375×812로 리사이즈 → 방 생성 → 대기실(`/rooms/YORR64/lobby`) 진입 시 `document.documentElement.scrollWidth`(425px) `>` `clientWidth`(375px) — 페이지 전체가 가로로 밀림.
- 원인: [`src/components/InvitationPanel.tsx:55-63`](src/components/InvitationPanel.tsx:55)의 QR+텍스트 가로 배치(`flex items-stretch gap-4`)에서, 방 코드를 담은 `<div className="grid gap-1">`(라인 56)에 `min-w-0`이 없음. 부모는 `min-w-0 flex-1`이라 줄어들 수 있지만, CSS Grid 아이템은 기본적으로 `min-width: auto`(= 내용 기준 최소폭)라 `font-mono text-4xl`(36px) 방 코드 문자열의 렌더링 폭보다 더 줄어들지 않는다. 결과적으로 QR(7.375rem 고정) + gap-4 + 방 코드 열의 최소폭 합이 375px보다 커져 행 전체가 넘친다.
- 재현 폭 확인: 6자 코드(`YORR64`) 기준으로도 이미 넘침 발생 — 방 코드는 최대 12자([`src/roomCode.ts`](src/roomCode.ts) `ROOM_CODE_MAX_LENGTH`)까지 가능해 실제로는 더 크게 넘칠 수 있음.
- 영향: `current-baseline.md`에 명시된 지원 기기(갤럭시 Chrome·iPhone Safari)의 좁은 화면에서 대기실 진입 시 카드가 잘리거나 페이지가 가로로 밀리는 사용자 경험 저하.
- 제안: 방 코드를 담은 grid 컨테이너(또는 `YORR64` span)에 `min-w-0`을 추가하고, 필요하면 좁은 화면에서 폰트 크기를 한 단계 낮추거나(`text-3xl` 등) `break-all`/`truncate`를 함께 고려.
- 참고: 같은 화면의 게임 진행(`/rooms/YORR64/game`) 페이지는 375px에서 가로 넘침 없음 — 대기실 전용 이슈.
- (2026-07-30 추가) 사용자가 직접 QA에서 짚은 6번("모바일에서 불필요한 좌우·상하 스크롤")이 이 항목과 동일 증상으로 확인됨 — 아래 "사용자 제보 재검증" 절 참고.

## 사용자 제보 재검증 (2차 QA)

> 사용자가 직접 플레이하며 짚어낸 6건을 재검증. mock 환경(단일 클라이언트)은 상대 플레이어의
> 실제 굴림·제출을 만들어낼 방법이 없어(아래 "재검증 방법" 참고), 브라우저 콘솔에서 앱과 같은
> 모듈 인스턴스(`useAppStore`)를 가져오고, `App.tsx`에 임시로 `window.__qaClient`(FakeRealtimeClient
> 참조)를 노출해 실제 서버가 보낼 `round.start`/`dice.broadcast`/`score.update` 메시지를 그대로
> 주입하는 방식으로 라이브 재현했다. **임시 프로브는 재검증 후 `git checkout`으로 완전히 되돌렸고
> 커밋에 포함하지 않았다** — 이 세션에서 유일하게 커밋한 파일은 `.dev.md`뿐이다.

### 겹치는 항목 정리
- 사용자 6번("모바일에서 불필요한 좌우·상하 스크롤")은 **FND-4**(대기실 가로 스크롤)와 동일 증상 — 새 항목으로 만들지 않고 FND-4에 통합.
- 사용자 4번("모바일에서 UI가 깨지거나 스크롤이 화면을 다 채움")은 재검증 결과 **FND-4** + 아래 **FND-6**(대기실 세로 스크롤, 5번과 같은 원인)로 구성됨 — 별도 항목 추가하지 않음.

### FND-5. 상대 턴 종료 후 점수시트가 자동으로 닫히지 않음 (실제 버그, 확정 재현) — 사용자 제보 1번
**상태: 수정 완료** — activePlayerId 변경 이펙트에 `setSheetOpen(false)` 추가. vitest로 라이브 재현과 같은 시나리오(3회 굴림 관전 → 턴 복귀 → 시트 닫힘)를 회귀 테스트로 고정. [`GamePlay.tsx`](src/screens/GamePlay.tsx)
- **재현(라이브, mock에 `round.start`/`dice.broadcast`/`score.update` 직접 주입)**: 참가자 턴으로 전환 → 참가자가 3회 굴림 마침(점수시트 자동 오픈, `aria-expanded="true"`) → 참가자가 점수 확정(`score.update`+`round.end`) → 턴이 나에게 복귀(`round.start`, 헤더는 "내 턴이에요"로 정확히 갱신됨) → 그런데 점수시트는 계속 `aria-expanded="true"`로 열린 채 유지됨. 시트 타이틀도 "기록 — 나"로 바뀌지만 닫히지는 않음.
- 원인: [`src/screens/GamePlay.tsx:69`](src/screens/GamePlay.tsx:69) `sheetOpen`을 닫는 경로는 딱 하나 — 라인 218, **내가 직접 제출한** `round.submit`의 응답(`score.update` + `pending.msgId` 일치)일 때뿐. 턴 주인이 바뀌는 걸 감지하는 [`GamePlay.tsx:101-108`](src/screens/GamePlay.tsx:101) 이펙트(`activePlayerId` 변경 시 로컬 굴림 상태 리셋)에는 `setSheetOpen(false)`가 없다. 즉 **내 턴이 아닐 때(관전) 열린 시트는 그 어떤 경로로도 자동으로 닫히지 않는다** — 사용자가 손으로 아래로 끌어내리거나 바깥을 탭해야만 닫힘.
- 라인 462-465의 자동 오픈 이펙트(`local.rollCount >= MAX_ROLLS`일 때 `setSheetOpen(true)`)는 `isMyTurn` 여부를 따지지 않고 걸려 있어 상대 턴에도 그대로 동작한다 — 관전 중 상대의 점수판을 보여주려는 의도(타이틀이 "기록 — {상대 닉네임}"으로 바뀜)까지는 맞지만, 닫는 짝이 빠져 있다.
- 제안: [`GamePlay.tsx:101-108`](src/screens/GamePlay.tsx:101)의 activePlayerId 변경 이펙트에 `setSheetOpen(false)`를 추가 — "턴이 넘어가면 로컬 상태를 리셋한다"는 기존 의도와 정확히 같은 자리이므로 자연스럽게 맞물린다.

### FND-6. 대기실이 세로로도 페이지 전체 스크롤됨 (실제 버그) — 사용자 제보 5번
**상태: 수정 완료(대기실·결과 화면 모두)** — `<main>`을 `h-svh overflow-hidden`으로, 참가자 목록 section을 `min-h-0 flex-1 overflow-y-auto`로 변경. 6인 채운 상태로 페이지 스크롤 0, 목록만 내부 스크롤 확인. GameResult도 후속 구현에서 같은 패턴으로 고정(랭킹 목록만 스크롤, 375×812 6인 실측 페이지 스크롤 0). [`LobbyPage.tsx`](src/screens/LobbyPage.tsx), [`GameResult.tsx`](src/screens/GameResult.tsx)
- **재현(라이브, 스토어에 6인 스냅샷 주입)**: 375×812에서 참가자를 6/6으로 채우면 `document.documentElement.scrollHeight(942) > clientHeight(812)` — 대기실 `<main>` 전체가 세로로 밀려 브라우저 기본 스크롤이 걸린다. `main`도 참가자 목록 `<section>`도 `overflow-y: visible`이라 내부 스크롤 컨테이너가 없다.
- 원인: [`src/screens/LobbyPage.tsx:63`](src/screens/LobbyPage.tsx:63) `<main>`이 `min-h-dvh`(최소 높이만 고정)로 되어 있어, 참가자가 많아지면 컨텐츠가 뷰포트보다 커지고 그대로 페이지 스크롤로 흘러간다. 참가자 목록 섹션([`LobbyPage.tsx:113`](src/screens/LobbyPage.tsx:113))에 별도 `max-height`/`overflow-y-auto`가 없다.
- 비교: [`GamePlay.tsx:826`](src/screens/GamePlay.tsx:826)의 게임 화면은 `<main className="h-svh overflow-hidden ...">`로 프레임을 고정하고 점수시트(`RecordPanel`) 내부에서만 스크롤이 나게 설계되어 있다 — 대기실만 이 패턴을 따르지 않아 사용자가 요청한 "목록만 스크롤" 구조와 어긋난다.
- 부수 확인: `GameResult`(결과 화면)도 같은 `min-h-dvh` 패턴이라 6인 랭킹에서 페이지가 세로로 늘어난다(`scrollHeight 905 > clientHeight 812`). 다만 결과 화면은 한 번 보고 넘어가는 화면이라 체감 우선순위는 대기실보다 낮다고 판단 — 같은 원인이라 별도 FND로 쪼개지 않고 여기 병기.
- 제안: `LobbyPage`의 `<main>`을 `GamePlay`처럼 `h-svh overflow-hidden` 뼈대로 바꾸고, 참가자 목록 섹션에 `min-h-0 flex-1 overflow-y-auto`를 줘서 그 부분만 스크롤되게 한다. 초대 카드·시작 버튼은 고정 영역으로 유지.

### FND-7. 턴 전환 시각 신호가 약함 — 사용자 제보 2번
**상태: 수정 완료(a·b·c 전부)** — 1차에서 보수적 기본안(폰트 확대·`transition-colors`·펄스)을 적용했고, 후속 구현에서 세 옵션을 모두 반영했다: a) TurnStrip 활성 카드 진입 애니메이션(`turn-pop`), b) 헤더 턴 라벨이 턴 주인 변경 시 리마운트되며 짧게 번쩍임(`turn-flash`), c) 라운드가 바뀌면 관전자에게도 "라운드 N 시작 — OO의 턴이에요" 토스트(내 턴 알림과 중복 방지, 라운드 시작으로 한정). 회귀 테스트 1건 추가. [`TurnStrip.tsx`](src/components/TurnStrip.tsx), [`GamePlay.tsx`](src/screens/GamePlay.tsx), [`tokens.css`](src/styles/tokens.css)
- **재검증(라이브)**: 참가자 턴으로 전환했을 때 실제로 바뀌는 건 ①헤더의 12px~15px 텍스트("참가자의 턴", `text-[15px] font-semibold`, 옆에 7px 점 하나 — [`GamePlay.tsx:588-613`](src/screens/GamePlay.tsx:588))와 ②상단 `TurnStrip`의 활성 카드 테두리/배경 틴트([`TurnStrip.tsx:41-47`](src/components/TurnStrip.tsx:41))뿐. 색이 바뀌는 순간에 트랜지션(`transition-colors` 등)도 없고, 토스트·진동·플래시 같은 "전환됐다"는 별도 신호가 전혀 없다.
- 비교: 내 턴이 "시작"될 때만 [`useMyTurnAlert`](src/screens/GamePlay.tsx:1013)가 토스트+진동(`vibrateForMyTurn`)을 준다 — 그런데 이건 "내 턴이 됐을 때"만이고, 관전 중 남의 턴이 다른 사람에게 넘어가는 경우엔 아무 알림도 없다. 즉 지금 있는 유일한 "턴 전환 강조"조차 내 턴 시작 케이스로 한정돼 있다.
- 이건 버그라기보다 사용자가 요청한 신규 UX(연출 강화)에 가깝다 — 코드를 고치기보다 우선 방향을 확인하는 게 맞다고 판단해 구현하지 않고 아래처럼 옵션만 정리해둔다:
  - a) `TurnStrip`의 활성 카드에 진입 애니메이션(스케일 펄스 등) 추가
  - b) 헤더 턴 텍스트에 짧은 flash/컬러 트랜지션 추가
  - c) 관전 중에도 "OO 턴 시작" 토스트를 띄우되, 너무 잦으면 피로감을 주므로 라운드 시작 시에만
  - 어느 걸 원하는지, 정도(토스트 vs 애니메이션 vs 둘 다)를 먼저 정하면 그때 구현하는 게 낫다고 본다.

### FND-8. 웹(와이드) 화면 간 폭 기준이 제각각 — 사용자 제보 3번
**상태: 수정 완료** — 닉네임·잘못된 코드 화면을 `max-w-lg`(512px)에서 `max-w-2xl`(672px)로 올려 대기실·결과 화면과 통일. 1280px에서 4개 화면 모두 672px 확인. [`NicknamePage.tsx`](src/screens/NicknamePage.tsx), [`InvalidInvitePage.tsx`](src/screens/InvalidInvitePage.tsx)
- **재검증(라이브 측정, 1280px 뷰포트)**: 같은 세션 흐름 안에서 화면마다 `<main>` 렌더 폭이 다르다.
  - 닉네임 페이지 [`NicknamePage.tsx:62`](src/screens/NicknamePage.tsx:62) `max-w-lg` → **512px**
  - 대기실 [`LobbyPage.tsx:63`](src/screens/LobbyPage.tsx:63) `max-w-2xl` → **672px**
  - 결과 화면 [`GameResult.tsx:47`](src/screens/GameResult.tsx:47) `max-w-2xl` → **672px**
  - 잘못된 초대 코드 화면 [`InvalidInvitePage.tsx:27`](src/screens/InvalidInvitePage.tsx:27) `max-w-lg` → 512px (닉네임과 짝은 맞음)
  - 게임 플레이 화면(와이드)은 `max-w-*` 자체가 없이 전체 폭을 그대로 씀([`GamePlay.tsx:827`](src/screens/GamePlay.tsx:827) `grid-cols-[1fr_32.5rem]`) — 의도된 몰입형 레이아웃이라 문제 없음
- 랜딩(`EntryPage`)도 폭 제한 없이 풀블리드 — 이것도 히어로 레이아웃이라 의도된 것으로 보임.
- 정리하면: "입장 전(닉네임/잘못된 코드)"은 512px, "입장 후(대기실/결과)"는 672px로 **두 그룹의 기준이 다르고 근거가 코드에 없음** — 닉네임(512px) → 대기실(672px) 전환 시 카드 폭이 눈에 띄게 넓어지는 게 사용자가 말한 "폭이 각기 다름"에 해당한다고 판단.
- 제안: 두 값 중 하나로 통일(예: 전부 `max-w-2xl` 또는 전부 `max-w-lg`)하거나, 의도적으로 다르게 갈 것이면(폼 화면은 좁게, 정보 화면은 넓게) 그 기준을 주석으로 남겨 다음에 폭을 추가/수정할 때 실수로 어긋나지 않게 한다.

### FND-9. (덤 발견) "참가자이 굴리는 중" — 이/가 조사 오류
**상태: 수정 완료** — `NicknamePage.tsx`의 "(으)로" 표기 관례를 따라 "(이)가"로 변경(받침 유무 판정 로직 없이 문구로 대응). [`GamePlay.tsx`](src/screens/GamePlay.tsx)
- 재검증 중 라이브 재현 화면에서 발견: [`GamePlay.tsx:690`](src/screens/GamePlay.tsx:690) `` `${activePlayer.nickname}이 굴리는 중` `` 이 "이"를 하드코딩. 닉네임이 받침 없는 글자로 끝나면("참가자", "펭귄" 등 임의 닉네임 다수) "참가자이"처럼 어색해진다. 올바르게는 "참가자가".
- 같은 파일 라인 809의 `${nickname}님이 굴리는 중입니다`는 "님"에 받침(ㅁ)이 있어 "이"가 맞아 문제 없음 — 라인 690만 해당.
- 사용자 제보 목록에는 없지만 재검증 과정에서 눈에 띄어 기록. 닉네임 마지막 글자의 받침 유무로 이/가를 고르는 작은 유틸 함수 하나로 해결 가능.

## 상세 로그

### S1. 랜딩 페이지 — 통과
- [x] 앱 기동, MSW mock 모드 활성(serviceWorker controller 확인)
- [x] 와이드(1280px) 레이아웃: 게임 탭 5종(요트 다이스 live, 나머지 준비 중), "방 만들기" CTA, 코드 입력 폼 노출
- [x] 빈 코드: 참가 버튼 disabled
- [x] `ab!c-1` 입력 → `ABC1`로 즉시 정규화(대문자·영숫자만), 4자 이상이라 버튼 활성
- [x] 초대 링크 통붙여넣기 `https://yorr.app/join?code=yorr64` → `YORR64` 추출
- [x] code 파라미터 없는 URL 붙여넣기 → 빈 값 처리(가짜 코드 생성 안 함)
- [x] 유효 코드 참가 → `/join?code=YORR64` 이동

### S2. 초대 코드 참가 경로 — 통과
- [x] `/join?code=YORR64` → 닉네임 페이지(초대 코드 배지 표시)
- [x] `/join?code=AB`(형식 오류) → InvalidInvitePage, 입장 요청 미발송 안내
- [x] 코드 `YORR64`로 수정 후 "수정한 코드로 참가" → 닉네임 페이지 정상 진입 (FND-2 문구 불일치)

### S3. 닉네임 검증 — 통과(문구 불일치 1건)
- [x] 자동 제안 placeholder 노출("재빠른 해적" 등 형용사+명사 조합)
- [x] 특수문자 `약탈자!!` → "닉네임에는 문자, 숫자, 공백만 사용할 수 있어요." 에러, 미진입
- [x] 13자 입력 → "닉네임은 12자 이하로 입력해 주세요." 에러 (input maxLength=13로 초과 입력 1자 허용해 에러 노출 — 의도된 패턴으로 보임)
- [x] 1자 닉네임 "가" → 통과·입장 (FND-1)

### S4. 방 생성 → 로비 — 통과
- [x] 대기실 진입: 방 코드·초대 링크(QR 포함) 노출, 참가 인원 "2/6"(mock 고정 스냅샷), "링크 복사" 동작
- [x] "게임 시작" 버튼: 방장 세션에서는 활성, 클릭 시 `/rooms/YORR64/game`으로 이동
- [x] 나가기 확인 모달 → "나가기" 선택 시 세션 정리(sessionStorage의 `yorr.room-session` 삭제) 후 랜딩으로 복귀, 복귀 배너 미노출 확인

### S5. 게임 플레이 코어 루프 — 통과(FND-3)
- [x] 방장 턴 진입: 타이머 25→0 카운트다운, "내 턴이에요" 표시, 모션 센서 온보딩 모달(닫기 가능)
- [x] 굴리기 1~3회: `굴림 n/3` 갱신, 남은 굴리기 카운트 갱신
- [x] 주사위 1·2번 킵(단축키 `1`,`2`) → "킵 레일 · 2/5 · 합 11" 반영
- [x] 3회 소진 후 "굴림 소진" 배지, 굴리기 버튼 자동 비활성화
- [x] 특수 족보 하이라이트 연출("라지 스트레이트!!") 정상 표시
- [x] 0점 카테고리(요트) 선택 → "요트를 0점으로 확정할까요?" 확인 모달, 취소 시 미반영
- [x] 점수 있는 카테고리(라지 스트레이트) 확정 → "점수가 반영됐습니다" 토스트, 점수표 반영(FND-3: 헤더 라벨 전이 상태 불일치)

### S6. 세션 복원·나가기 — 통과(mock 한계 1건)
- [x] `sessionStorage`에 `yorr.room-session`(roomId·sessionToken·최신 snapshot 포함) 저장 확인
- [x] 게임 진행 중 URL `/rooms/YORR64/game`으로 콜드 리로드 → 랜딩으로 리다이렉트, "진행 중인 방이 있어요" 배너("이어서 하기") 노출. 세션 없이 방 URL 직접 접근을 막는 [`src/screens/RoomExitGuard.tsx`](src/screens/RoomExitGuard.tsx)/FSM 정책과 일치
- [x] "이어서 하기" 클릭 → `resumeRoomSession()` 후 FSM이 계산한 화면으로 이동(이번 실행은 대기실로 이동 — mock 재접속 스냅샷 한계 때문, 위 참고 항목 참조)
- [x] 대기실에서 "나가기" → 확인 모달 → 확정 시 `sessionStorage` 정리, 랜딩 복귀, 배너 사라짐 (S4에서 별도 확인 완료)

### S7. 오류 경로 — 통과
- [x] 존재하지 않는 라우트(`/this-route-does-not-exist`) → 404 페이지, 시도한 경로 표시, "홈으로" 링크
- [x] 형식은 유효하나 실재하지 않는 방 코드(`NOPE99`)로 참가 시도 → "존재하지 않거나 더 이상 사용할 수 없는 방이에요." 에러, "다른 코드 입력" 옵션, 세션은 생성되지 않음
- [x] mock REST 404 응답이 plain text(`room_not_found`)인데도 [`src/api/client.ts`](src/api/client.ts)의 `textErrorPayload` 매핑으로 `ROOM_NOT_FOUND` 코드 정상 변환 → [`src/api/userError.ts`](src/api/userError.ts) 메시지 정상 노출

### S8. 모바일 뷰포트 반응형 — 버그 1건(FND-4)
- [x] 375×812로 리사이즈, 랜딩 페이지: 칩 레일 + 바텀시트 구조 정상, 가로 스크롤 없음
- [x] 방 만들기 → 닉네임 → 대기실 전 구간 모바일 폭에서 네비게이션 정상
- [x] **대기실 화면에서 가로 스크롤 발생 확인(FND-4, 버그)** — `scrollWidth 425 > clientWidth 375`
- [x] 게임 화면(`/rooms/:roomId/game`)은 모바일 폭에서 가로 스크롤 없음, 점수 시트가 바텀시트 형태로 정상 전환
- 참고(자동화 도구 이슈, 앱과 무관): `resize_window(preset: mobile)` 이후 `computer.left_click`의 좌표 클릭이 실제로 반영되지 않는 현상 관찰(같은 좌표를 `element.click()`으로 재현하면 정상 동작). Browser 자동화 도구의 좌표계 이슈로 추정 — 앱 버그 아님, 추후 QA 시 모바일 폭에서는 `computer` 클릭 대신 JS dispatch를 우선 사용 권장.

## 수정 구현 (2026-07-30)

> QA에서 확인된 발견사항을 파일/관심사 단위로 쪼개 5개의 작은 커밋으로 나눠 반영했다.
> 각 FND 항목 위쪽에 붙인 "**상태**" 줄이 해당 발견사항의 최신 처리 결과다 — 이 절은 "왜
> 이렇게 고쳤는지"와 "무엇을 일부러 안 건드렸는지"를 모아서 보는 용도다.

### 설계 원칙
- **한 커밋 = 한 관심사.** 같은 파일(`GamePlay.tsx`)을 건드린 FND-3·FND-5·FND-9만 예외로
  묶었다 — 셋 다 "턴/제출 상태 표시"라는 같은 문맥의 코드 블록을 고치는 거라 쪼개면 오히려
  리뷰하기 어려워진다고 판단했다. 나머지(FND-4, FND-6, FND-8, FND-1/2)는 파일과 관심사가
  달라 각각 별도 커밋으로 뒀다.
- **검증 로직이 아니라 표시를 고친 것**: FND-1·FND-2는 "닉네임 1자·초대코드 4자 허용"이라는
  기존 검증 규칙 자체는 그대로 두고, 안내 문구만 규칙에 맞췄다. 최소 길이 정책을 바꾸는 건
  기획 결정이라 이번 범위에서 임의로 바꾸지 않았다.
- **레이아웃 버그는 항상 최소재현(6자·2인)과 최악치(12자·6인) 양쪽으로 확인**: FND-4는 현재
  코드(`YORR64`, 6자)뿐 아니라 `ROOM_CODE_MAX_LENGTH`(12자)까지, FND-6은 기본 2인뿐 아니라
  정원(6/6)까지 스토어에 직접 주입해 재현한 뒤 고쳤다. 화면에 보이는 값만 보고 고치면 나중에
  긴 값에서 다시 터진다.
- **FND-7(턴 전환 연출)은 절반만 구현**: "무엇을 만들지" 자체가 기획 결정이라, 이미 있는
  디자인 토큰(`animate-ring-pulse`, `transition-colors`)만 재사용하는 보수적인 확대·펄스만
  적용하고 토스트·애니메이션 같은 더 큰 변경은 옵션으로만 남겼다. 근거: CLAUDE.md의 "디자인이
  확정되지 않은 상태에서 pixel-perfect 작업을 임의로 확대하지 않는다" 원칙.

### 완료된 부분
| 커밋 | 내용 | 검증 방법 |
|---|---|---|
| `fix: 상대 턴 종료 후 점수시트 자동 닫힘 및 헤더 상태 표시 개선` | FND-3, FND-5, FND-9 | vitest 3건 신규 + 기존 14건 통과, typecheck |
| `fix: 대기실 초대 카드가 모바일 폭에서 가로로 넘치던 문제 수정` | FND-4 | 브라우저에서 6자·12자 코드 375px 실측(scrollWidth === clientWidth) |
| `fix: 대기실 참가자 목록만 스크롤되도록 프레임 고정` | FND-6(대기실만) | 기존 vitest 7건 통과 + 브라우저에서 2인/6인 실측 |
| `fix: 웹 화면 간 카드 폭 기준을 max-w-2xl로 통일` | FND-8 | 기존 vitest 12건 통과 + 1280px 실측(4화면 모두 672px) |
| `fix: 닉네임·초대코드 안내 문구를 실제 검증 규칙에 맞춤` | FND-1, FND-2 | 기존 vitest 3건 통과 + 브라우저 텍스트 확인 |

모두 `npx tsc -b`(전체 typecheck)와 해당 파일 `biome check` 통과, 관련 vitest만 실행(전체
스위트는 실행하지 않음 — CLAUDE.md "변경 범위와 직접 관련되면 해당 테스트만 우선 실행" 원칙).

### 남은 부분
~~1차 구현에서 남겨뒀던 세 항목(FND-6 GameResult, FND-7 강한 연출, mock 픽스처 한계 2건)은
전부 아래 "후속 구현" 절에서 완료했다.~~ 현재 남은 것은 선택 항목 하나뿐:
- **375px 뷰포트 스모크 E2E**(제안): 모바일 레이아웃 회귀가 걱정되면 `frontend/e2e/`에
  추가할 수 있다. frontend/CLAUDE.md의 E2E 최소화 원칙에 따라 명시적 요청 전까지는 보류.

## 후속 구현 (2026-07-30, 2차)

> 1차에서 "방향 확인 필요"로 남겨뒀던 항목을 전부 구현했다. 커밋 3개.

| 커밋 | 내용 | 검증 방법 |
|---|---|---|
| `fix: 결과 화면도 랭킹 목록만 스크롤되도록 프레임 고정` | FND-6 GameResult | 기존 vitest 통과 + 375×812 6인 실측(scrollHeight 812 = clientHeight 812, 랭킹 ol만 내부 스크롤 364>271, 하단 버튼 3개 고정 노출) |
| `feat: 턴 전환 연출 강화 — 카드 팝·헤더 플래시·라운드 시작 토스트` | FND-7 a·b·c | vitest 1건 신규(관전자 라운드 토스트, 첫 렌더 미발화) + 브라우저에서 computed `animation-name`(turn-pop·turn-flash) 및 토스트 문구 실측 |
| `test: mock 서버가 방 상태와 제출 점수를 실제처럼 기억하도록 개선` | mock 한계 2건 | vitest 2건 신규(재접속 스냅샷 복원·점수 누적 저장) + 기존 테스트 갱신, 브라우저에서 게임 시작→새로고침→"이어서 하기"→게임 화면·점수판 복원 실측 |

추가 요청(사용자): **내 차례 알림을 하단 토스트 대신 족보 이펙트처럼** — `RollResultCallout`의
연출부를 `EffectCallout`(임의 문구 + tier)으로 분리하고, 내 턴 시작 시 트레이 위에 "내 차례!"
콜아웃(tier 2: 팝 + 확산 링)을 띄운다. 진동은 유지, 토스트는 제거. 기존 회귀 테스트를 콜아웃
기준으로 갱신했고, 브라우저에서 표시·자동 소멸(1.8s)·토스트 미표시를 실측했다.
[`RollResultCallout.tsx`](src/components/RollResultCallout.tsx), [`GamePlay.tsx`](src/screens/GamePlay.tsx)

구현 메모:
- **mock 방 상태는 `sessionStorage`(`yorr.mock-room-state`)에 둔다** — 새로고침을 넘어
  살아남아야 재접속 시나리오가 성립하기 때문. 게임 시작(REST)이 저장하고, `room.join`이
  새 마감 시각과 함께 복원하며, 대기실 복귀·나가기가 지운다. `round.submit`은 제출된
  주사위로 `domain/scoring`을 사용해 점수를 계산(소계·보너스·총점 누적)한 뒤 같은 저장소에
  반영한다. 정적 `scoreCandidates` 픽스처는 사용처가 없어져 삭제했다.
- FND-7c 토스트는 **라운드 번호가 실제로 바뀔 때만** 발화한다(첫 렌더·중간 입장·재접속은
  전환이 아님). 내 턴 시작은 기존 `useMyTurnAlert`가 맡으므로 관전 중일 때만 띄운다.
- 전체 vitest 스위트 243건 통과(mock은 여러 화면 테스트가 공유하는 인프라라 이번에는
  전체를 돌렸다). `tsc -b`·biome 통과.
- 브라우저 검증 중 물리 주사위 굴림은 이번에도 완료되지 않았다(1차 한계점과 동일 —
  Browser pane 미표시 상태에서 rAF가 돌지 않음). 제출 경로는 vitest로, 재접속 복원은
  mock 상태를 직접 심어 실측했다.

### 한계점
- **모바일 물리 주사위 애니메이션이 이 브라우저 자동화 환경에서 안정적으로 완료되지 않았다**
  (`requestAnimationFrame`/WebGL이 보이지 않는 pane에서 제대로 안 돌아가는 것으로 추정).
  그래서 FND-3·FND-5는 실제 3D 굴림 대신 ①vitest의 기존 `PhysicsDiceScene` mock(굴림 완료
  버튼으로 대체)과 ②브라우저 콘솔에서 `useAppStore`를 동적 import하고 `App.tsx`에 임시로
  노출한 `window.__qaClient`로 실제 서버 메시지(`round.start`/`dice.broadcast`/`score.update`)
  를 직접 주입하는 방식으로 검증했다. 임시 프로브는 검증 후 전부 `git checkout`으로 되돌렸다
  — 이 브랜치에 커밋된 코드에는 남아있지 않다.
- **실서버(fallback/실제 백엔드) 연동 상태에서는 재검증하지 않았다.** 이번 수정·검증은 전부
  MSW `mock` 모드(REST=MSW, WS=FakeRealtimeClient) 기준이다. 실서버가 `round.start`를 조금이라도
  늦게 보내면 FND-3의 "제출 완료 · 대기 중" 문구가 보이는 시간이 사람이 체감할 정도로 길어질 수
  있는데, 그 실제 체감 지연은 이 환경에서 측정할 수 없었다.
- **FND-8(폭 통일)은 시각적 균형까지 확인하지 않았다.** 1280px에서 렌더 폭이 672px로 같아진
  것만 수치로 확인했고, 닉네임 화면처럼 입력 필드 하나뿐인 화면이 672px 카드 안에서 휑해
  보이는지 같은 "느낌"은 디자인 리뷰가 아니라서 판단하지 않았다.
- **모바일 회귀 테스트는 전부 수동(브라우저 스크립트) 측정이었다.** Playwright E2E나 CSS
  회귀 스냅샷으로 자동화하지 않았다 — frontend/CLAUDE.md의 "Playwright E2E는 실제 사용자
  흐름·다중 사용자 상태·브라우저 호환성이 티켓 범위에 명시된 경우에만" 원칙에 따라, 이번
  수정 규모(레이아웃 유틸리티 클래스 위주)에서는 과한 검증이라고 판단했다. 회귀가 걱정되면
  후속으로 `frontend/e2e/`에 375px 뷰포트 스모크 테스트를 추가하는 걸 제안한다.
