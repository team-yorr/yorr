# S15P11A406-84 작업 노트

> 티켓: [S15P11A406-84](https://ssafy.atlassian.net/browse/S15P11A406-84) — 미리 정의된 값으로 굴림 결과가 나오게 3d 렌더링 모듈 변경
> 브랜치: `feature/S15P11A406-84-deterministic-roll-result` (base: `develop` @ `32bad31`)
> 상태: 구현 완료 · 검증 통과 (typecheck / vitest 193개 / check / build / 데모 시각 확인) · 담당: 정유진 · 우선순위: Low
> 티켓 본문 설명이 비어 있어, 아래 요구사항은 사용자와의 설계 논의로 확정한 내용이다.

---

## 요구사항 (논의로 확정)

1. **결과값은 게임 로직이 사전에 결정한다.** 주사위 굴림·확정은 전적으로 프론트엔드 처리.
   렌더러는 "정해진 값이 우연히 나온 것처럼" 보여야 한다.
2. **쪼는 맛이 핵심 가치다.** 3D를 쓰는 이유가 마지막에 천천히 구르는 긴장감이므로,
   유저가 지켜보는 정착 순간 이후에 눈이 바뀌는 연출은 실패로 간주한다.
   → 눈 교체는 주사위가 빠르게 회전하는 동안 끝나야 한다.
3. **멀티플레이 동기화는 "값 동기화(수준 1)"만.** `targetDice`를 공유하면 각 클라이언트가
   자기 물리로 굴려도 최종 눈은 전부 동일하다. 궤적까지 동일하게(수준 2, 결정론 락스텝)는
   이번 범위 밖 — 단 이번 결정론 리팩터가 그 토대가 된다.

### 검토 후 기각된 대안

| 대안 | 기각 사유 |
|---|---|
| 시뮬레이션 중 개입(각속도 조향) | 최종 면은 카오스계라 닫힌 수식이 없어 결국 예측 시뮬 필요 + 개입 자체가 눈에 띔 + 실패 시 보정 필요. 모든 접근의 비용만 합쳐진다 |
| 정착 후 정렬 때만 교체 (단독안) | 구현 최소지만 쪼는 맛의 보상 순간(정착 직후)에 눈이 바뀌는 게 보임. 요구 2 위반. **안전망으로는 유지** |
| WebRTC 화면 브로드캐스트 | 굴리는 폰이 인코딩+업로드 부담, STUN/TURN 인프라 신규, 압축 화질·지터로 쪼는 맛 훼손. 주사위 굴림은 재현 가능한 콘텐츠라 재생 정보 몇 바이트가 항상 이긴다 |

---

## 현재 구조 (변경 전)

결과가 물리의 창발적 산물이다 — 이 티켓과 정확히 반대 방향.

- `World.ts:489` `startResultAlignment`가 정착 후 `readTopDice()`로 윗면을 읽어 그걸 결과로 확정,
  `onRollComplete(requestId, settledDice)`로 보고.
- `request.seed`는 초기 배치·임펄스 난수에만 쓰인다. 같은 시드라도 흔들기 시간(wall-clock)에
  따라 결과가 달라진다.
- `domain/dice.test.ts:22`에 `targetDice` 속성이 **없어야 한다**는 테스트가 있다
  ("creates roll metadata without planning dice values") — 과거의 의도적 결정. 이 티켓이 뒤집는다.
- 2D 폴백(`PhysicsDiceFallback`)은 시드로 자체 굴림 — 3D 물리와 **다른 값**이 나오는 잠재 불일치.
  targetDice 도입이 이 불일치도 해소한다.

## 핵심 원리 — 큐브 대칭 비주얼 리맵

정육면체는 24개 회전 대칭이 있다. **물리 바디는 손대지 않고 비주얼 메쉬만 대칭 회전 `R`만큼
돌려 끼우면**(`mesh.quaternion = body.rotation × R`) 역학은 100% 진짜인데 멈췄을 때 윗면이
목표값이 된다. 회전한 주사위는 항상 유효한 주사위이므로 눈 배치 모순이 생기지 않는다.

`R`은 목표면 법선 `n_t`를 자연 결과면 법선 `n_v`로 보내는 회전: `R·n_t = n_v`.
축 정렬 단위벡터 간이라 90°(수직) 또는 180°(반대) 회전이고 둘 다 큐브 대칭군 원소다.

교체 타이밍이 전부다: **쏟아진 직후 공중에서 빠르게 회전할 때** 오프셋을 적용하면 인지 불가능.

## 설계 — 두 층 구조

**정확성은 안전망이 보장하고, 예측·리맵은 순수 연출 계층.** 예측이 빗나가도 최종 값은 항상
`targetDice`로 수렴하며, 최악의 비용은 "정렬 회전이 조금 큼"뿐이다.

### 층 1 — targetDice 주입 + 정렬 안전망

- `DiceRollRequest` / `PhysicsDiceRollRequest`에 `targetDice` 추가.
- 값 생성은 domain 몫: `rollDice(seed, held, currentDice)` 순수 함수 신설
  (held 주사위는 현재 값 유지). LCG는 기존 `advanceSeed`와 같은 상수 —
  기존 2D 폴백과 같은 난수 스트림이라 값 호환.
- 정착 후 정렬(`startResultAlignment`)이 `readTopDice()` 대신 `request.targetDice`를
  정렬 목표로 사용. `onRollComplete`도 `targetDice` 보고.
- 정렬 트윈(`prepareAlignmentEntries`)은 이미 이동+확대+회전을 동시에 하므로
  예측 실패 시의 큰 회전도 이 연출 속에 흡수된다.

### 층 2 — 쏟아짐 시점 예측 + 공중 리맵

- **결정론 리팩터 (전제)**: `containDiceInTray` 호출을 rAF 프레임 단위에서
  고정 물리 스텝 루프(`while accumulator`) 안으로 이동. 예측 시뮬과 실제 진행이
  프레임레이트와 무관하게 같은 궤적을 그리게 하는 유일한 변경.
- `releaseFromBowl()`에서 임펄스 적용 직후(이후 난수 소비·외부 개입 없음):
  1. `world.takeSnapshot()` → `RAPIER.World.restoreSnapshot()`으로 월드 복제
  2. 복제본을 렌더링 없이 고정 스텝으로 완주 (동일한 트레이 경계 보정 포함, 스텝 상한 존재)
  3. 각 주사위의 자연 결과면을 `topFaceFromQuaternion`으로 읽음
  4. `cubeAlignmentOffset(target, natural)`로 오프셋 `R` 계산, `DieEntry.visualOffset`에 저장
- 렌더 루프의 메쉬 동기화가 `body.rotation × visualOffset`을 사용 — 적용 순간 주사위는
  공중에서 빠르게 회전 중이라 교체가 보이지 않는다.
- 정렬 완료 시 **오프셋 베이크**: body는 이미 `quaternionForTopValue(target)`으로 고정되므로
  `visualOffset`을 identity로 리셋. held·다음 굴림 경로의 회전 정합 유지.
- 예측 실패(스텝 상한 초과, 복제 실패)면 오프셋 없이 층 1이 그대로 동작.

### 멀티 동기화 관점 (범위 밖, 기록만)

`targetDice`를 브로드캐스트하면 각 클라이언트의 물리·리맵은 전부 로컬이어도 최종 눈이 동일하다.
JS `Math.sin` 엔진별 미세 차이로 궤적이 갈라져도 각자 로컬 리맵이라 값은 절대 안 틀린다 —
정확성이 물리 결정론에 의존하지 않는 것이 이 설계의 멀티플레이 이점.

---

## 변경 지점

| 파일 | 변경 |
|---|---|
| `domain/dice.ts` | `rollDice()` 신설, `DiceRollRequest.targetDice`, `createRollRequest`에 `currentDice`(필수)·`targetDice`(선택 오버라이드) 입력 추가 |
| `domain/yachtGame.ts` | `requestRoll`이 `currentDice: state.dice` 전달 |
| `rendering/physics-dice/types.ts` | `PhysicsDiceRollRequest.targetDice` |
| `rendering/physics-dice/runtimeTypes.ts` | `DieEntry.visualOffset: THREE.Quaternion` |
| `rendering/physics-dice/diceInstances.ts` | `visualOffset` 초기화 |
| `rendering/physics-dice/model.ts` | `faceNormalForValue()` export |
| `rendering/physics-dice/remap.ts` **신규** | `cubeAlignmentOffset()` · `predictNaturalDice()` (복제 시뮬) |
| `rendering/physics-dice/safety.ts` | `containDiceInTray`를 body 단위 최소 shape로 일반화 (복제 월드에서 재사용) |
| `rendering/physics-dice/World.ts` | 경계 보정 스텝 루프 이동, 예측 호출, 메쉬 동기화에 오프셋, 정렬 목표 `targetDice`, 베이크 |
| `rendering/physics-dice/result.ts` | 삭제 — `readTopDice`는 결과 소스에서 제외 (예측이 `topFaceFromQuaternion` 직접 사용) |
| `components/PhysicsDiceFallback.tsx` | 자체 굴림(`rollFallbackDice`) 제거 → `request.targetDice` 표시 |
| `app/PhysicsDiceDemo.tsx` | `currentDice` 전달, 목표값 강제 지정 입력(검증용 dev 도구) |

## 검증 계획

변경 위험에 비례해서만 (`frontend/CLAUDE.md` 테스트 최소화 원칙).

- **`remap.test.ts` 신규** — 이 티켓의 완료 조건 그 자체인 순수 수학:
  36개 (natural, target) 조합 전부에 대해 `R·n_t = n_v`이고 `R`이 큐브 대칭(90°/180°/identity)인지.
- **`dice.test.ts` 의도 반전** — `targetDice`가 "없어야 한다" → "있어야 하고 held를 보존한다".
- 기존 physics-dice·Scene·Fallback 테스트 통과 확인.
- `npm run typecheck` · `npm run check`.
- 데모 화면에서 목표값 강제 지정 후 굴림 → 최종 눈 일치 + 연출 자연스러움 시각 확인.

```bash
npm run typecheck
npx vitest run src/domain src/rendering src/components/PhysicsDice
npm run check
```

## 커밋 계획

파일 경계가 커밋 경계가 되도록 나눈다 (각 커밋 단독으로 컴파일·테스트 통과).

1. `docs: S15P11A406-84 작업 노트 작성` — `.dev.md`
2. `feat: 굴림 요청에 사전 결정 목표값(targetDice) 도입` — domain(`rollDice` · 계약 변경) + wire 타입 + 2D 폴백 + 테스트 픽스처. 이 시점의 3D 렌더러는 아직 물리 결과를 쓴다.
3. `feat: 예측 리맵으로 물리 굴림이 목표 눈으로 정착하게 변경` — 렌더링 모듈 전체 (`World.ts` 정렬 안전망 + 결정론 리팩터 + `remap.ts` 예측·오프셋 + `result.ts` 삭제 + `remap.test.ts`)
4. `chore: 주사위 데모에 목표값 강제 지정 도구 추가` — `PhysicsDiceDemo.tsx`

MR 제목: `feat: 미리 정의된 값으로 굴림 결과가 나오는 3D 주사위` (`develop` ← 이 브랜치)
