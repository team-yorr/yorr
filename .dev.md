# S15P11A406-91 작업 노트

> 티켓: [S15P11A406-91](https://ssafy.atlassian.net/browse/S15P11A406-91) — 모션센서 닫기 버튼 추가하기 && 프론트 게임 시작 조건 1인 부터로 변경
> 브랜치: `feature/S15P11A406-91-sensor-close-and-solo-start` (base: `develop` @ `db96a23`)
> 상태: 진행 중 · 담당: 정유진 · 우선순위: Low
> 티켓 본문 설명이 비어 있어, 아래 요구사항은 코드 조사로 확정한 내용이다.

---

## 사전 조사에서 확정된 사실

작업 방향을 좌우하는 항목만 적는다.

1. **백엔드는 이미 1인 시작을 허용한다.**
   `RoomValidationService.START` Lua 스크립트가 `HLEN(players) < 1`일 때만 거부한다
   (`backend/.../room/service/RoomValidationService.java:47`). 즉 인원 1명이면 통과한다.
   → **이 티켓은 프론트엔드 변경만으로 완결된다.** 백엔드 수정 불필요.

2. **1인 게임이 도중에 강제 종료되지 않는다.**
   `GameAbortService.abortForNotEnoughPlayers`가 `room.closed / NOT_ENOUGH_PLAYERS`를 브로드캐스트하지만,
   백엔드 전체에 **호출자가 없는 미사용 코드**다. 1인 진행 중 방이 닫힐 위험은 현재 없다.

3. **MSW mock에도 인원 게이트가 없다.**
   `POST /api/v1/rooms/:roomCode/games` 핸들러는 roomCode만 검사한다
   (`frontend/src/mocks/restHandlers.ts:52`). mock 수정 불필요.

---

## 작업 1 — 모션센서 안내 패널에 닫기 버튼 추가

### 문제

`MotionPermissionPanel`은 주사위 화면 위에 절대 위치로 덮인다
(`frontend/src/screens/GamePlay.tsx:281-288`, `absolute inset-x-3 top-3 z-30`).

노출 조건은 `isPermissionNoticeState(motion.availability)` — 즉
`permissionRequired · requesting · denied · error · insecure` 5개 상태
(`GamePlay.tsx:666-676`).

이 중 `denied · error · insecure`는 **되돌릴 수 없는 종료 상태**다. 이미 탭 모드로 폴백되어
게임은 정상 진행되는데, 안내 패널만 주사위 위를 영구히 가린다. 닫을 방법이 없다.

### 요구사항

- 패널에 닫기 버튼을 추가해 사용자가 직접 치울 수 있게 한다.
- 닫아도 게임 진행에는 영향이 없어야 한다 (패널은 안내 전용, 입력 모드와 무관).
- 닫은 뒤에도 센서 상태는 하단 힌트 문구로 계속 확인 가능해야 한다
  → 이미 `keyboardHint`가 `getGestureMessage()`로 같은 정보를 그린다 (`GamePlay.tsx:325-331`). 추가 작업 없음.

### 변경 지점

| 파일 | 변경 |
|---|---|
| `frontend/src/components/MotionPermissionPanel.tsx` | `onClose?: () => void` prop 추가, 두 분기 모두에 닫기 버튼 렌더 |
| `frontend/src/screens/GamePlay.tsx` | 닫힘 상태 `useState` 소유, `onClose` 전달 |

### 설계 결정 (구현 완료)

- **상태 소유는 `GamePlay`.** 패널은 표시 전용으로 두고, 닫힘 여부는 화면이 관리한다.
  패널이 자기 표시 여부를 스스로 지우면 부모의 `isPermissionNoticeState` 조건과 진실이 두 곳으로 갈라진다.
- **닫힘 상태를 boolean이 아니라 "닫은 `availability` 값"으로 들고 있는다.**
  ```tsx
  const [dismissedNotice, setDismissedNotice] = useState<MotionAvailability | null>(null)
  // 표시 조건
  isPermissionNoticeState(motion.availability) && dismissedNotice !== motion.availability
  ```
  boolean + 초기화 `useEffect` 조합보다 낫다. `permissionRequired → denied`처럼 상황이 바뀌면
  비교값이 저절로 어긋나 새 안내가 다시 뜬다. **초기화 effect 자체가 필요 없어진다.**
  (`GamePlay.tsx:81`의 라운드 전환 처리와 같은 결의 "렌더 중 파생" 패턴이다.)
- **`onClose`는 optional이 아니라 required로 뒀다.** 호출자가 하나뿐이고,
  이 패널은 화면을 덮으므로 "닫을 수 없는 사용처"를 애초에 허용할 이유가 없다.
- **닫기 버튼은 두 분기(권한 요청 / 종료 안내) 모두에 넣었다.** 분기마다 다르게 굴면 예측이 어렵다.
- 접근성: `type="button"`, `aria-label="센서 안내 닫기"`, `size-tap`.
  모양은 `Modal.tsx:60`의 닫기 버튼과 맞췄다 — 같은 동작은 같은 모양이어야 한다.

### 열린 질문 → 결론

- `permissionRequired`에서 닫으면 그 상태가 유지되는 동안 권한 허용 진입점이 사라진다.
  **재진입 수단은 넣지 않았다.** 티켓 범위는 "닫기 버튼 추가"까지이고,
  하단 힌트(`getGestureMessage`)가 센서 상태를 계속 알려주므로 정보가 사라지지는 않는다.
  실사용에서 문제가 되면 별도 티켓으로 다룬다.

---

## 작업 2 — 게임 시작 조건 2인 → 1인

### 변경 지점

`frontend/src/screens/LobbyPage.tsx` 단일 파일.

**확정: 요트 다이스 사용 인원은 1–6인.**

| 파일 | 위치 | 현재 | 변경 후 |
|---|---|---|---|
| `LobbyPage.tsx` | :25 | `roomSnapshot.players.length >= 2` | `>= MIN_PLAYERS_TO_START` (= 1) |
| `LobbyPage.tsx` | :99 | `'2명부터 시작할 수 있어요.'` | 상수를 쓴 문구로 통일 |
| `landingGames.ts` | :18 | 요트 다이스 `players: '2–6인'` | `'1–6인'` |

### 설계 결정

- **`MIN_PLAYERS_TO_START = 1` 상수를 파일 상단에 둔다.**
  조건식과 안내 문구 두 곳에 숫자가 흩어져 있는 게 지금 버그의 모양이다
  (조건은 `>= 2`, 문구는 `'2명부터'` — 하나만 고치면 어긋난다). 한 곳에서 파생시킨다.
- 안내 문구 분기 자체는 유지한다. 호스트는 항상 `players`에 포함되므로 실질적으로 도달하지 않는 분기가 되지만,
  스냅샷이 비어 있는 경계 상황을 위해 남긴다.

### 확인된 부수 영향

- `ScorePanel`은 `players.length > 1`로 이미 1인을 처리한다 (`ScorePanel.tsx:98,136`). 수정 불필요.
- `GamePlay`의 대기 문구는 1인일 때 "1명 중 1명 완료 · 다음 라운드를 기다리는 중"으로 표시된다
  (`GamePlay.tsx:306`). 어색하지만 오동작은 아니다 → **이번 범위 밖**, 필요하면 별도 티켓.

---

## 범위 밖 (건드리지 않음)

- 백엔드 코드 — 위 조사 1·2에 따라 변경 불필요.
- `GameAbortService` 미사용 코드 정리.
- **`capacity` 서버 연동** — 아래 부록 참고. 별도 티켓으로 분리한다.

---

## 검증 계획

변경 위험에 비례해서만 돌린다 (`frontend/CLAUDE.md` 테스트 최소화 원칙).

```bash
npm run typecheck
npm test -- LobbyPage MotionPermissionPanel
npm run check
```

테스트 조정 (완료):

- `LobbyPage.test.tsx` `'lets the host start once two players are present'`
  → `'lets the host start the game'`으로 이름 수정. 낡은 규칙을 주장하는 이름이었다.
- **`'lets the host start alone'` 추가** — 이 티켓의 완료 조건 그 자체이고,
  상수를 2로 되돌리면 깨져야 하는 회귀 지점이다.
- **`MotionPermissionPanel.test.tsx` 신규** — 두 분기 각각의 닫기 interaction. 기존 테스트가 없던 컴포넌트다.

E2E·모바일 실기기 검증은 티켓에 명시되지 않았으므로 돌리지 않았다.

### 실행 결과

| 명령 | 결과 |
|---|---|
| `npm run typecheck` | ✅ 통과 |
| `npx vitest run` (LobbyPage · MotionPermissionPanel · GamePlay · GamePage) | ✅ 17개 통과 |
| `npm run check` | ⚠️ 아래 1건 외 통과 |

`npm run check`가 `.claude/settings.local.json` 포맷을 지적하지만
**전역 gitignore 대상**(`**/.claude/settings.local.json`)이라 커밋에 포함되지 않는다.
이번 변경과 무관한 로컬 파일이므로 건드리지 않았다.

---

## 커밋 계획

작업 두 개를 분리한다.

1. `feat: 모션 센서 안내 패널에 닫기 버튼 추가`
2. `fix: 게임 시작 최소 인원을 1명으로 변경`

MR 제목: `feat: 모션 센서 안내 닫기 버튼 및 1인 게임 시작 지원` (`develop` ← 이 브랜치)

---

# 부록 — 게임이 늘어날 때 게임별 설정을 어떻게 받아올 것인가

> 이 티켓의 범위가 아니다. 지금 결정할 필요는 없지만, **이 티켓 자체가 그 문제의 증상**이라
> 근거를 남겨둔다. 실제 착수는 두 번째 게임이 붙을 때.

## 이 티켓이 드러낸 진짜 문제

프론트는 `players.length >= 2`로 시작을 막고 있었다. 그런데 백엔드는 `HLEN < 1`,
즉 **처음부터 1명이면 허용**하고 있었다.

**프론트가 서버가 강제하지도 않는 규칙을 혼자 하드코딩하고 있었다.**
게임이 하나뿐인 지금도 규칙이 두 곳으로 갈라져 어긋났다. 게임이 5개가 되면 이 어긋남은 5배가 된다.

같은 모양의 하드코딩이 지금 프론트에 흩어져 있다:

| 값 | 위치 | 서버가 강제하나? |
|---|---|---|
| 최소 인원 | `LobbyPage.tsx:25` | ✅ (`HLEN < 1`) — **어긋나 있었음** |
| 최대 인원 | `LobbyPage.tsx:67` `'최대 6명'` | ✅ (`capacity`) — **서버가 보내는데 프론트가 버림** |
| 총 라운드 | `GamePlay.tsx:48` `TOTAL_ROUNDS = 12` | ❓ 미확인 |
| 굴림 횟수 | `GamePlay.tsx:49` `MAX_ROLLS = 3` | ❓ 미확인 |
| 인원·소요시간 카피 | `landingGames.ts` | ❌ 순수 표시용 |

## 이미 있는 이음매 — `capacity`

서버는 방 정원을 **이미 스냅샷에 실어 보내고 있다.**

- `backend/.../room/dto/RoomSnapshot.java:5` — `int capacity`
- `backend/.../RoomCreateService.java:32` — 방 생성 시 `capacity`를 인자로 받아 Redis에 저장
- `frontend/src/mocks/restHandlers.ts:88` — mock도 `capacity: 6` 반환

그런데 프론트가 버린다:

- `frontend/src/realtime/wsEvents.ts:83` `RoomSnapshot` 타입에 `capacity` 필드가 없다
- `frontend/src/api/gameApi.ts:185` `toRoomSnapshot`이 매핑하지 않는다
- 그래서 `LobbyPage.tsx:67`이 `'최대 6명'`을 하드코딩한다

**즉 "서버에서 받아 설정한다"의 첫 걸음은 새 설계가 아니라, 이미 오는 값을 안 버리는 것이다.**

## 소유권 원칙 — 누가 강제하느냐가 누가 소유하느냐다

새 게임을 붙일 때 설정값을 세 부류로 나눈다.

**1. 서버가 거부할 수 있는 값 → 반드시 서버에서 받는다**
정원, 최소 인원, 라운드 수, 제한 시간처럼 위반하면 서버가 요청을 튕기는 값.
프론트가 추측하면 이번 티켓과 똑같은 버그가 난다 — 화면은 "2명부터"인데 서버는 1명을 받는 상태.

**2. 순수 표시용 값 → 프론트가 소유한다**
게임 이름, 소개 문구, 아이콘, 소요시간 카피. `landingGames.ts`가 이미 이 역할을 한다.
서버에 넣으면 카피 한 줄 고치는 데 배포가 필요해진다.

**3. 렌더러·입력기 선택 → 프론트가 `gameKey`로 분기한다**
주사위 씬이냐 탁구 씬이냐. 서버는 `gameKey`만 보내면 되고, 컴포넌트 매핑은 프론트 소유다.
`HeroGameKey` union(`landingGames.ts:5`)이 이미 그 키다.

## 권장 형태

방 스냅샷에 `gameKey`와 서버가 강제하는 규칙만 싣는다.

```ts
// wsEvents.ts — 계약의 SSOT
export interface RoomSnapshot {
  roomId: RoomId
  phase: RoomPhase
  players: Player[]
  gameKey: GameKey        // 어떤 게임인지
  rules: GameRules        // 서버가 강제하는 값만
  game?: GameState
}

export interface GameRules {
  minPlayers: number
  maxPlayers: number      // 지금의 capacity
  // 라운드·굴림 규칙은 서버 강제 여부 확인 후 추가
}
```

프론트는 표시용 정보만 `gameKey`로 조회한다.

```ts
// landingGames.ts를 확장하거나 별도 게임 카탈로그로
const gameDisplay: Record<GameKey, { name: string; icon: ...; scene: ... }>
```

그러면 `LobbyPage`는 이렇게 된다 — 숫자 리터럴이 사라진다.

```tsx
const canStart = /* ... */ && roomSnapshot.players.length >= roomSnapshot.rules.minPlayers
// '현재 인원 N / 최대 6명' → rules.maxPlayers
// 'N명부터 시작할 수 있어요.' → rules.minPlayers
```

## 왜 지금 하지 않나

- 게임이 하나뿐이라 추상화의 정답을 알 수 없다. 두 번째 게임의 실제 요구가 설계를 결정해야 한다.
- `RoomSnapshot`은 WebSocket 계약의 SSOT라 필드 추가에 백엔드 합의가 필요하다 —
  `wsEvents.ts:87`, `:95` 주석대로 `GameState`는 아직 STUB이고 소유자가 따로 있다.
- 이 티켓(Low)에 끼워 넣으면 MR이 커진다. `frontend/CLAUDE.md`의 "MR은 작게" 원칙에 어긋난다.

## 착수 순서 제안 (별도 티켓)

1. **`capacity`부터 연결한다.** 서버가 이미 보내는 값이라 백엔드 합의가 필요 없다.
   `wsEvents.ts` 타입 + `gameApi.ts` 매핑 + `roomSessionStorage.ts` 검증 + `LobbyPage.tsx:67` 표시.
   가장 싸고, 하드코딩 하나를 실제로 없앤다.
2. `TOTAL_ROUNDS` · `MAX_ROLLS`를 서버가 강제하는지 확인한다. 강제한다면 `GameRules`로 승격.
3. 두 번째 게임 티켓이 열릴 때 `gameKey` + `GameRules`를 계약에 추가한다.
