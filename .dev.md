# S15P11A406-105 작업 노트

> 티켓: [S15P11A406-105](https://ssafy.atlassian.net/browse/S15P11A406-105) — ui 수정
> 브랜치: `feature/S15P11A406-105-game-ui-update` (base: `develop` @ `274cc09`)
> 상태: 구현 완료 · 검증 통과 (typecheck / check / vitest 195개 / build / 브라우저 확인) · 담당: 정유진
> 레퍼런스는 모션 데브 사이트 프로토타입 스크린샷으로 추정 (DEV 단축키 힌트 노출).
> 티켓 원문: "여기서 3d 부분만 지금 구현으로 하고 외형은 해당 레퍼런스를 따라 간다"
> → **3D 트레이는 현재 구현 유지, 나머지 외형은 레퍼런스를 따른다.**

---

## 레퍼런스 요약 (첨부 5장)

| # | 내용 | 계획에 주는 정보 |
|---|---|---|
| 1 | 점수시트 (498×1052) — 모든 족보 행 라벨 앞에 주사위/눈 아이콘 | 상단(Aces~Sixes)은 해당 눈 1~6 주사위, 하단(Choice·4 Kind·풀하우스·스트레이트·Yacht)도 각자 pip 패턴 아이콘 |
| 2 | 진입 화면 — 다크 배경, "요트 다이스 / YACHT DICE" 타이틀, 닉네임·방 만들기·게임 참여 | 앱 전반의 다크+골드 스타일 방향 |
| 3 | 게임 화면 — 우측 점수 패널(UPPER/LOWER 2단, 한글 족보명), 상단 중앙 타이틀, R 1/12, 하단 ●●○ 남은 굴리기 + 대형 CTA | 게임 화면 외형의 핵심 기준 |
| 4 | 게임 화면 + 이펙트 — 트레이 위 대형 골드 "요트!!!" 텍스트, CTA "다시 굴리기 · 2번 남음" | 이펙트 = 한글 족보명 + 느낌표, 골드 아웃라인 대형 텍스트 |
| 5 | 아케이드 게임 선택 화면 (YORR ARCADE 캐러셀) | 스타일 방향 참고 (이번 티켓 범위는 게임 화면 우선) |

**확정된 스타일 방향**: 웜 다크(진갈색·블랙) 배경 + 골드/크림 액센트, 라운드 카드 패널,
letter-spacing 있는 영문 캡션(UPPER / LOWER / TOTAL / YACHT DICE) + 한글 본문.

**확정된 족보 한글 표기** (레퍼런스 3 그대로):
에이스 · 듀스 · 트레이 · 포 · 파이브 · 식스 | 초이스 · 포커(4 of a Kind) · 풀하우스 ·
스몰 스트레이트 · 라지 스트레이트 · 요트

---

## 현재 코드 상태

### 이펙트 (항목 1)

- `components/RollResultCallout.tsx` — 문구 전부 영문:
  `captionByHand` `'Nice Roll'/'Great Roll'/'Jackpot'`(:23), 본문 `categoryLabel[hand]` 영문(:87).
  현재는 테두리 박스+캡션 구조 — 레퍼런스 4는 박스 없는 대형 골드 텍스트다.
- tier 1~3 연출(팝/링/플래시·버스트)과 리마운트 구조(`GamePlay.tsx:465`)는 유지 가능.

### 점수칸 (항목 2)

- 게임 점수시트는 `components/ScoreSheet.tsx` 하나 — 모바일 바텀시트·웹 우측 패널 공용.
  족보 라벨은 텍스트만(:118). 결과 화면은 `components/ScoreMatrix.tsx`.
- `components/Dice.tsx` 최소 sm=56px — 행 높이(min-h-11)에 맞는 소형 사이즈가 없다.
  하단 족보용 pip 패턴 아이콘(풀하우스·스트레이트 등)은 아예 없다 — 신설 필요.

### UI 외형 (항목 3)

- `screens/GamePlay.tsx` — 헤더(좌측 YACHT 텍스트+스탯 나열) · 3D 트레이 · 하단 CTA 바 ·
  모바일 RecordPanel / 웹 우측 점수시트 패널(520px, 직선 보더).
  레퍼런스는 중앙 타이틀 헤더 + 라운드 카드 점수 패널 + ●●○ 굴림 인디케이터 + 대형 골드 CTA.
- 남은 굴림 표시는 현재 `RollCounter`(트레이 우상단) — 레퍼런스는 하단 좌측 도트 3개.
- 디자인 토큰은 `styles/`의 Tailwind v4 `@theme` semantic token — 다크+골드 팔레트로 값 교체.
- 3D 트레이(`rendering/physics-dice/*`, `--ds-physics-tray`)는 **수정 금지**.

---

## 작업 계획

### 1. 이펙트 한글화 + 레퍼런스 스타일

- `yachtCategoryView.ts`에 한글 족보 표기 맵 추가 (위 확정 표기).
- `RollResultCallout.tsx`:
  - 본문을 `한글 족보명 + '!!!'` 대형 골드 텍스트로 교체 (레퍼런스 4: 외곽선 있는 골드,
    박스 제거). `uppercase`·`tracking` 등 영문 전제 스타일 제거.
  - 영문 캡션(`captionByHand`)은 레퍼런스에 없음 — 제거 (tier 구분은 연출 강도로만 유지).
  - tier별 팝/링/플래시·버스트 연출과 duration은 유지.

### 2. 점수칸에 주사위 아이콘 추가

- 소형 족보 아이콘 컴포넌트 신설 (`components/CategoryIcon.tsx` 가칭, ≈16~20px):
  - 상단 6종: 해당 눈(1~6)의 주사위 면 — `Dice.tsx`의 dot 좌표 로직 재사용/축소
  - 하단 6종: 레퍼런스 1의 pip 패턴 (초이스·포커·풀하우스·S/L 스트레이트·요트)
- `ScoreSheet.tsx` 족보 행 라벨 앞에 아이콘 배치. 첫 열 폭(`minmax(5.25rem,1.3fr)`)을
  아이콘+한글 라벨 기준으로 재점검 (320px 2열에서 줄바꿈 없어야 함).
- `ScoreMatrix.tsx`(결과 화면)도 같은 아이콘+한글 라벨로 통일.

### 3. UI 외형 — 다크+골드 테마 반영 (게임 화면 우선)

- `styles/` 토큰: canvas/surface/border/content를 웜 다크 계열로, brand를 골드로 교체.
  기존 semantic token 이름은 유지하고 값만 바꿔 전 화면에 전파.
- `GamePlay.tsx`:
  - 헤더: 중앙 "요트 다이스 / YACHT DICE" 타이틀 + 좌측 ‹ 나가기 + 우측 `R n/12` 배지
  - 하단 바: ●●○ 남은 굴리기 도트(좌) + "주사위를 탭하면 고정" 힌트(우) + 대형 골드 CTA
    (굴리는 중… / 다시 굴리기 · n번 남음 문구 반영)
  - 우측 점수 패널: 라운드 카드 스타일. **레퍼런스의 UPPER/LOWER 2단 배치는 1인 기준** —
    다인전 플레이어 열 구조는 유지하고 카드·타이포·보너스 프로그레스바 스타일만 따른다.
    (2단 배치 시도는 폭 제약 확인 후 별도 판단)
- 족보 라벨 한글화가 점수시트·퀵 칩·토스트·0점 모달에 전파된다 —
  `categoryLabel`/`categoryShortLabel`을 한글로 교체하고 라벨 단언 테스트 동반 수정.
- 진입(레퍼런스 2)·아케이드 선택(레퍼런스 5) 화면은 토큰 교체로 자연히 어두워지는 범위까지만.
  전용 리디자인은 이번 티켓에서 하지 않는다 (범위 확대 시 별도 티켓).

---

## 변경 지점 (예상)

| 파일 | 변경 |
|---|---|
| `yachtCategoryView.ts` | `categoryLabel`·`categoryShortLabel` 한글화 |
| `components/RollResultCallout.tsx` | 대형 골드 한글 텍스트로 개편, 캡션 제거 |
| `components/CategoryIcon.tsx` **신규** | 족보 12종 소형 아이콘 (주사위 면 + pip 패턴) |
| `components/ScoreSheet.tsx` | 행 아이콘 추가, 카드 스타일, 보너스 프로그레스 |
| `components/ScoreMatrix.tsx` | 아이콘+한글 라벨 통일 |
| `components/RollCounter.tsx` | ●●○ 도트 스타일 (하단 바 이동 여부 포함) |
| `screens/GamePlay.tsx` | 헤더·하단 바·패널 프레임 레퍼런스 반영 |
| `styles/*` | 다크+골드 토큰 값 교체 |
| `ScoreRow.test.tsx` 등 | 라벨 문자열 단언 수정 |

## 검증 계획

frontend/CLAUDE.md 테스트 최소화 원칙 — 문구·정적 스타일 변경이 대부분이므로:

- `npm run typecheck` · `npm run check` · `npm run build`
- 라벨을 단언하는 기존 테스트만 선별 실행:
  `npx vitest run src/components src/screens/GamePlay.test.tsx src/yachtCategoryView.test.ts`
- 시각 확인: dev 서버에서 모바일(375px)·웹(1280px) 두 레이아웃을 레퍼런스 3·4와 대조
  (외형 반영이 이 티켓의 완료 조건이므로 시각 검토 수행)

## 커밋 계획

요구사항 항목 = 커밋 경계. 각 커밋 단독으로 typecheck·테스트 통과.

1. `docs: S15P11A406-105 작업 노트 작성` — `.dev.md`
2. `feat: 족보 표기 한글화 및 굴림 이펙트를 골드 텍스트로 개편` — 표기 맵 + RollResultCallout (+테스트)
3. `feat: 점수시트 족보 행에 주사위 아이콘 추가` — CategoryIcon/ScoreSheet/ScoreMatrix
4. `feat: 게임 화면을 다크·골드 레퍼런스 외형으로 조정` — 토큰/GamePlay/RollCounter

MR: `develop` ← 이 브랜치, 제목 `feat: 게임 화면 UI 수정 — 한글화·점수칸 주사위·다크 골드 외형`
